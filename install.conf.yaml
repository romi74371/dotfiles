- create:
    - ~/.config
    - ~/.tmux/plugins
- link:
    ~/.zshrc: .zshrc
    ~/.gitconfig: .gitconfig
    ~/.aliases: .aliases
    ~/.functions: .functions
    ~/.vimrc: .vimrc
    ~/.tmux.conf: .tmux.conf
    ~/.tool-versions: .tool-versions
    ~/.config/starship.toml: .config/starship.toml
    ~/.config/nvim: .config/nvim
- shell:
  - command: brew bundle --file=$BREWFILE
    description: Install packages
  - command: git clone git@github.com:tmux-plugins/tpm.git ~/.tmux/plugins/tpm
    description: Clone tmux plugin manager
